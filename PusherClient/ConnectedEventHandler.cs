namespace PusherClient
{
    /// <summary>
    /// The Event Handler for the Connected Event on the <see cref="Channel"/>
    /// </summary>
    /// <param name="sender">The object that connected</param>
    public delegate void ConnectedEventHandler(object sender);
}